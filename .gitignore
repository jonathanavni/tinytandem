# Secrets — never commit
.env
.env.*

# Idea funnel — committed by default so the skeleton shows the pattern.
# Uncomment to keep it local instead (recommended if you work across multiple git
# worktrees, where a committed BACKLOG drifts between copies).
# BACKLOG.md

# Dependencies / build output (uncomment what applies to your stack)
node_modules/
# dist/
# build/
# __pycache__/
# .venv/

# OS / editor
.DS_Store
*.log
