# Decisions

Non-obvious product and architecture decisions, with the reasoning. The kind of thing a future session needs to *not* re-litigate or accidentally undo.

<!-- One entry per decision. Format:
- **<short title>** — <what was decided>. Rejected <alternative> because <reason>. (<date>)

Example:
- **Cursor pagination, not offset** — the upstream API doesn't honor offset reliably past page 50, so all paginated clients use cursor-based pagination. (2026-01-12)
-->
